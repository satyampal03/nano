
const arr = [343,4,5,4545,4,5,5,5,45,45,45,4,15,14,5]

// getting last array element
console.log(arr[arr.length-1]);
console.log(arr.slice(-1)[0]); //  -1 says that last elem of the array and put it at the 0th index  in a new array

console.log(arr.at(-3));  // this is the at method in the array that says that arrray ka indext that at position, there is the - index  so that array will work from the last of the array,

console.log(arr.splice(-1 )[0]);

// this will create a new arrray list and this will hold the value is the from previous aaray, of last element to  from last 4 index's values

